<h2><%= @plant.name %></h2>

<% unless @plant.image.nil? || @plant.image.empty? %>
  <img src="<%= @plant.image %>" alt="<%= @plant.name %>" class="feature">
<% end %>

<div class="controls">
  <a href="/plants/<%= @plant.id %>/edit">Edit plant</a>
  <a href="/plants/<%= @plant.id %>/delete">Delete plant</a>
</div>
